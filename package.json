{
  "name": "flarewatch",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "vp check --no-fmt --no-lint",
    "check": "vp check",
    "auth:secret": "node --experimental-strip-types scripts/generate-auth-secret.ts",
    "format": "vp fmt . --write",
    "format:check": "vp fmt . --check",
    "lint": "vp lint"
  },
  "devDependencies": {
    "eslint-plugin-react-hooks": "^7.1.1",
    "vite-plus": "^0.2.4"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.1.0"
}
